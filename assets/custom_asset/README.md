# custom_asset

Dossier réservé aux assets custom du sous-menu `Wall`.

Note : dans cette première version web, les imports utilisateur `.obj` sont stockés côté navigateur (localStorage) pour rester disponibles après rechargement de la page.
Ce dossier sert de point d’ancrage dans le repo pour la future pipeline d’assets custom.
